{
  "name": "herdr-omni",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "preversion": "bun run typecheck && bun test",
    "version": "bun run scripts/sync-version.ts && git add herdr-plugin.toml",
    "postversion": "git push --follow-tags"
  },
  "dependencies": {
    "@opentui/core": "^0.1.66"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.3"
  }
}
